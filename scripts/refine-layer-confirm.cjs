const fs=require('fs');const p='components/Composer.tsx';let s=fs.readFileSync(p,'utf8');s=s.replace('{layerConfirm && <div role="dialog" aria-modal="true"','{layerConfirm && operation==="layer-separation" && <div role="dialog"');s=s.replace('完成後按實際輸出張數結算，多退少不補；生成失敗退回。','每張 {credits === null ? "—" : credits / 17} 點，完成後按實際輸出張數結算，多退少不補；生成失敗退回。');s=s.replace('type="button" onClick={()=>submit(true)}>確認預扣並分離','type="button" disabled={!canSubmit} onClick={()=>submit(true)}>確認預扣並分離');fs.writeFileSync(p,s);
