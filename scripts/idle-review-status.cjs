const fs=require('fs');const p='components/CompanionIdleStage.tsx';let s=fs.readFileSync(p,'utf8');s=s.replace('  createdAt: string;','  createdAt: string;\n  needsReview?: boolean;\n  message?: string;');s=s.replace('  const buttonLabel = pending','  const needsReview = videos.find(v => v.needsReview);\n  const buttonLabel = needsReview ? "任務待核對" : pending');s=s.replace('        {error && <p','        {needsReview && <p role="status" className="mt-2 text-xs leading-relaxed text-[#e6c88f]">{needsReview.message || "任務提交結果待確認，請聯絡管理員；不會自動再次扣點。"}</p>}\n        {error && <p');fs.writeFileSync(p,s);
