require('@next/env').loadEnvConfig(process.cwd(),false,{info(){},error(){}});process.env.POSTGRES_URL ||= process.env.DATABASE_URL;const {sql}=require('@vercel/postgres');
(async()=>{const {rows:g}=await sql.query("select id,kind,model,prompt,url is not null as saved from generations where user_id=$1 and created_at >= $2 order by id",[1,'2026-09-08T09:40:00Z']);const {rows:c}=await sql.query('select count(*)::int as eligible from characters where user_id=$1 and affection>=80',[1]);console.log(JSON.stringify({generations:g.map(({prompt,...r})=>({...r,hasStageCap:prompt.includes('最終場景互動上限'),hasConflictingPreference:prompt.includes('互動界線：戀人般的互動')})),eligibleNsfwCharacters:c[0].eligible}));process.exit(0)})().catch(()=>{console.log('AUDIT_FAILED');process.exitCode=1});
