/** Navigation is shared by the authorized management shell. */
export default function AdminTabs({ active }: { active: "users" | "rates" | "models" | "home" | "landing" }) { void active; return null; }
