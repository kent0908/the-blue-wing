/** Verified live provider response contains only image URLs, without layer placement metadata.
 * Re-enable only after an end-to-end response contract verification; no environment bypass. */
export const LAYER_DECOMPOSITION_AVAILABLE = false;
export const LAYER_DECOMPOSITION_UNAVAILABLE_REASON = '圖層分離暫停開放：上游尚未提供圖層順序與位置資料，無法正確還原編輯。';
